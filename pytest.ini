[pytest]
markers =
    slow: training-based milestone gate tests (tens of seconds to minutes each).

# Fast loop (shapes/contracts/math, ~seconds):   pytest -m "not slow"
# Everything incl. milestone gates (~minutes):    pytest
